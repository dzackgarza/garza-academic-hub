# TODO

Tracked from thermo-nuclear code quality review (2026-05-24).

Every item follows TDD: write a failing test first (RED), commit it, then implement the
fix (GREEN), commit it.

* * *

- [x] **1. Remove duplicate `TypeDef` interface from `AcademicCollection.tsx`**
  - **RED**: write a test asserting that `TypeDef` imported from `FilterControls` is
    structurally identical to what `AcademicCollection` currently defines.
  - **GREEN**: delete the local `TypeDef` definition from
    `src/components/AcademicCollection.tsx` and import it from `FilterControls` instead.

- [x] **2. Replace `grid-cols-${columns}` template literal with a literal lookup map**
  - **RED**: write a test that given a rendered `PaginatedScroller` or `CardScroller`,
    the grid container's class list contains exactly the expected Tailwind literal (e.g.
    `grid-cols-2` or `grid-cols-3`) and does NOT contain an unresolved template
    expression.
  - **GREEN**: introduce a `Record<number, string>` lookup map in
    `src/components/PaginatedScroller.tsx` (and/or `CardScroller.tsx`) so every rendered
    class name is a literal string.

- [x] **3. Early-return in `FilterControls` when `filterable=false`**
  - **RED**: write a test rendering `FilterControls` with `filterable=false` that
    asserts no reactive filter state is maintained (the component should pass all items
    through unchanged).
  - **GREEN**: add an early `if (!filterable) return <>{children(items, 'all-')}</>;`
    guard at the top of the component body.

- [x] **4. Decouple `FilterControls` from `AcademicCardProps`**
  - **RED**: write a test rendering `FilterControls` configured with an explicit
    `TypeDef` that uses a `string` icon — it should render without importing
    `AcademicCardProps`.
  - **GREEN**: replace `AcademicCardProps['icon']` with `string` in
    `FilterControls.tsx:TypeDef.icon`; also apply the same change to
    `src/content/items.ts:TypeDef.icon`.

- [x] **5. Remove empty snap-start placeholder from `PaginatedScroller`**
  - **RED**: write a test rendering `PaginatedScroller` with an empty `items` array that
    asserts zero children are rendered inside the scroll container (i.e. no empty
    placeholder divs).
  - **GREEN**: replace the `pages.length === 0` branch in
    `src/components/PaginatedScroller.tsx` with `null` instead of an empty div.

- [x] **6. Make `ImageGallery` scroller columns/rows configurable via props**
  - **RED**: write a test rendering `ImageGallery` with `layout="scroller"`,
    `columns={3}`, `rows={2}` and asserting the `PaginatedScroller` receives those
    values.
  - **GREEN**: add optional `columns` and `rows` props to `ImageGalleryProps`, pass them
    through to `PaginatedScroller`, with current values as defaults.

- [x] **7. Type `resolveCollectionData` and REGISTRY slots with actual data shapes**
  - **RED**: write a type-level test (e.g. `expectTypeOf`) that a collection slot data
    object is rejected if it has unexpected fields beyond the known shape.
  - **GREEN**: introduce `CollectionSlotData` and `SlotItem` interfaces in
    `src/components/PageShell.tsx` and replace `any`/`Record<string, any>` usage
    accordingly.

- [x] **8. Extract shared nav-link rendering in `NavBar`**
  - **RED**: write a test asserting that adding a nav item to the data source
    automatically renders it in both desktop and mobile nav menus.
  - **GREEN**: extract a shared `renderNavLinks` helper (or inline map) in
    `src/components/NavBar.tsx` and use it in both the desktop `<nav>` and mobile
    `<nav>`.

* * *

## GOALS.md compliance gaps (2026-05-25)

- [x] **9. Remove hardcoded page routes from `vite.config.ts` sitemap (T2/T3)**
  - `vite.config.ts:52-62` hardcodes `/teaching`, `/activities`, `/writing`, `/gallery`,
    `/blog` in `dynamicRoutes` for `vite-plugin-sitemap`. This duplicates the sitemap
    already generated by `compile.cjs:345-355` from the manifest and violates the rule
    that the app must not encode page-specific route lists.
  - **RED**: `99b12f6` — `goals-contract.test.ts` asserts `vite.config.ts` has no
    hardcoded page path literals.
  - **GREEN**: `56f5e9c` — derives `sitemapRoutes` from `site-manifest.json` at Vite
    config time, removing all hardcoded route literals.

- [x] **10. Deduplicate shared template markup across page/post templates (T2)**
  - `page-template.html` and `post-template.html` duplicated identical `<nav>`,
    `<aside>`, and `<footer>` markup (~30 lines each).
  - **spec**: `58e0bd6` — test asserts nav/footer byte-identity across compiled pages
    and posts (already true, documents invariant).
  - **GREEN**: `f2dffd9` — merged both into `content/templates/site-template.html` using
    `$if(date)$` to switch between page and post layout in the `<main>` block.

- [x] **11. Remove route-specific content assertion branch from `tests/visual.spec.ts`
  (T4)**
  - `visual.spec.ts:56-60` had `if (route.path === '/')` with a hardcoded content string
    for the profile card affiliation.
  - **GREEN**: `3448140` — replaced with generic check verifying non-empty profile card
    on any route that uses the page template.
  - Remaining `route.path === '/'` at line 85 checks component hydration on the home
    page (the only page with AcademicCollection slots) — a legitimate behavioral
    assertion, not a content-boundary violation.

- [x] **12. Add blog posts to visual regression screenshot tests (T4)**
  - `tests/screenshots.spec.ts:21` filtered to `r.type === 'page'` only, excluding blog
    posts from visual regression.
    Old blog-post snapshot PNGs existed but were disconnected from the test.
  - **GREEN**: `79d67dd` — removed the `.filter((r) => r.type === 'page')` in
    `getRoutes()`. All 21 manifest routes are now exercised.

- [x] **13. Complete legacy asset migration (T6)**
  - Legacy surface inventory in `legacy-routes.toml` listed 6 asset roots; only
    `assets/images` had files in `public/`.
  - **RED**: `19e0047` — test enumerates asset roots from `legacy-routes.toml` and
    asserts none are empty or missing in `public/`.
  - **GREEN**: `0f5e597` — migrated real files from `~/website` into `public/` for
    `assets/pdfs`, `assets/talks`, `assets/teaching`, `assets/courses`, and `courses`.
